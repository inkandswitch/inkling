import m from "mithril"
import Vec from "./lib/vec"
import geom from "./lib/geom"

const state = {}

state.onmousedown = pos=>{}
state.onmousemove = pos=>{}
state.onmouseup = pos=>{}

window.addEventListener("keydown", e=>{})

window.addEventListener("keyup", e=>{})

export default state

